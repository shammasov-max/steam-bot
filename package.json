{
  "name": "steam-bot",
  "private": true,
  "packageManager": "yarn@4.9.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit",
    "build:server": "yarn workspace @steam-bot/server build",
    "build:frontend": "yarn workspace @steam-bot/frontend build",
    "start": "yarn workspace @steam-bot/server start"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "fastq": "^1.19.1"
  }
}
